# Punto de cierre actual — RPG MHA Custom (18+)

> Estado narrativo exacto + decisión pendiente del jugador. **Crítico para abrir sesión.**

## Estado narrativo

El proyecto está en **esqueleto inicial**. La carpeta existe, los `docs/` están poblados con placeholders y guías de tono, los `registros/` están vacíos, y no se ha definido el PC ni se ha jugado ninguna sesión.

**Tono decidido:** campaña **18+ adulta**. Todos los personajes (PC, NPCs canon, NPCs custom) tienen 18+ años. La campaña incluye **violencia explícita** y **sexualidad explícita** sin eufemismos. Ver `AGENTS.md` y `CLAUDE.md` para el detalle.

## Decisión pendiente del jugador

**Definir el PC** (personaje principal). Para ello, abrir `docs/sesion_cero.md` y rellenar:

1. Nombre, **edad 18+**, curso UA, quirk.
2. Tono y año de inicio. (Confirmar las líneas y velos.)
3. Arco inicial.

Tras eso:

1. Renombrar carpeta: `rpg-myheroacademia-pc-pendiente` → `rpg-myheroacademia-<nombre-pc-kebab>`.
2. Renombrar `docs/personaje_pc_pendiente.md` → `docs/personaje_<nombre>.md` y rellenarlo.
3. Poblar `registros/pj.csv` con la ficha inicial (con edad 18+).
4. Lanzar primera sesión.

## Próxima sesión

*(Por programar, una vez definido el PC.)*
